import React from 'react'
import Modal from '../../Modal/Modal'

const CardInfo = (props) => {
  return (
    <div>
      <Modal onClose = {()=>this.props.onClose()}><h>Hi There</h></Modal>
    </div>
  )
}

export default CardInfo
